#! /bin/bash

find "/mnt/public-server" > "$HOME/table/SERVER" 
find "/mnt/honsya-server" >> "$HOME/table/SERVER" 
find "/mnt/saitama-server" >> "$HOME/table/SERVER" 
find "/mnt/yokohama-server" >> "$HOME/table/SERVER" 
